package cl.uchile.dcc.scrabble.tipos.Operations;

/**
 * Represents the types that can be negated.
 */
public interface INegation {
    /**
     * Returns the negation (returns a new object).
     */
    INegation neg();
}
